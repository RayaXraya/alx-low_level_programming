#include <stdio.h>

/**
 * main - Entry point
 * Description: Laaa
 * Return: 0
 */

int main(void)
{
	int c, k;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	for (k = 'A'; k <= 'Z'; k++)
		putchar(k);
	putchar('\n');
	return (0);
}
